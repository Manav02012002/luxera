# geometry_healing_edge

Parity pack for geometry healing edge cases.

Each scene contains intentionally dirty custom surfaces and runs direct illuminance on a small grid with occlusion enabled. Expected outputs are locked after deterministic healing/report generation.
