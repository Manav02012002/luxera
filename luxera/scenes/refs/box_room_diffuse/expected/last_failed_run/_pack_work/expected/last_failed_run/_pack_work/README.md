# box_room_diffuse

Simple diffuse box room reference.

Validates:
- direct-only baseline (same scene, direct engine)
- radiosity bounce contribution magnitude
- convergence diagnostics (residual + energy balance)
