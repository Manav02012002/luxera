# Roadway Glare Pack

Intent:

- Regression guard for roadway glare outputs and deterministic observer contribution ordering.
- Validates that roadway payload includes glare worst-case summary under canonical `roadway.metrics`.

Expected focus:

- `roadway.method`
- `roadway.metrics.worst_case_glare`
- `summary.observer_glare_views[].contributions` deterministic order

